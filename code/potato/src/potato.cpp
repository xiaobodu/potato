#include "potato.h"

bool CreateEngine(ac::tool::IEngine*& rpEngine)
{
  if (rpEngine != NULL)
  {
    return false;
  }
  //TODO:
  return false;
}

bool DestroyEngine(ac::tool::IEngine*& rpEngine)
{
  if (rpEngine == NULL)
  {
    return false;
  }
  return false;
}
